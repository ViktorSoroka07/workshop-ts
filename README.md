## Types

TypeScript's type system built on top of JavaScript. Covers primitive types and type annotations, `let` vs `const` inference, type aliases and interfaces (and when to use which), function types and callbacks, union and intersection types, literal types, discriminated unions, type narrowing and type guards, enums vs literal unions, `any` vs `unknown`, and index signatures.

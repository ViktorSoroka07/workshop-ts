## The `satisfies` Operator

TypeScript's `satisfies` operator for type-safe value validation without widening. Covers the difference between implicit type inference, explicit type annotations, `as` assertions, and `satisfies` — and when to use each.

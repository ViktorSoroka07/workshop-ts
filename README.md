## Generics

Generic programming in TypeScript. Covers generic functions, generic interfaces and type aliases, generic classes, constraints with `extends`, `keyof` and indexed access types, mapped types (`Partial`, `Required`, `Readonly`, `Record`, custom mapped types), conditional types, `infer`, template literal types, and utility types.
